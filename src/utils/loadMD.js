// const fs = require('fs');

// function loadMarkdownFile(filePath) {
//     fs.readFile(filePath, 'utf8', (err, data) => {
//         if (err) {
//             console.error("Error al leer el archivo:", err);
//             return;
//         }

//         // Aquí tienes el contenido del archivo Markdown en la variable 'data'
//         const markdown = data;
//         return markdown
//     });
// }

